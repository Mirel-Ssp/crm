-- =====================================================================
-- CRM 系统 · V5 批次3补丁：商机备注列（CrmOpportunity.remark 映射）
-- V1 建表未含 remark，实体建档/编辑需读写备注（CRM-O1）
-- =====================================================================

ALTER TABLE crm_opportunity ADD COLUMN remark VARCHAR(500);

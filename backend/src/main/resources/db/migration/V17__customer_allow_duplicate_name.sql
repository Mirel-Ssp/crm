-- ==============================================================
-- V17 放开客户名称唯一性 + 查重接口增强
-- 原 uk_customer_name 阻止业务上完全合理的重名（如两家"北京科技"分属不同销售/不同地址/不同联系人）
-- 改为：允许同名存在；前端已有的软查重弹窗（checkDuplicate 接口）继续作为疑似重复提醒
-- 同时增强 checkDuplicate 返回字段，附带 region/address 辅助用户区分同名客户
-- ==============================================================

BEGIN;

-- 删掉 V1 建的 UNIQUE INDEX（仅对未删除记录生效的部分唯一索引）
DROP INDEX IF EXISTS uk_customer_name;

COMMIT;
